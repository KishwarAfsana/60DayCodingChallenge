#include <stdio.h>
int main (){
   float pie = 3.14;
   int radius = 6;
   printf("\nThe radius of circle is %d",radius);
   float area = (float)(pie* radius * radius);
   printf("\nThe area of the given circle is %f", area);
   return 0;
}
